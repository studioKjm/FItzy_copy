# 딥러닝 프레임워크
torch>=2.2.0
torchvision>=0.17.0
ultralytics>=8.0.0  # YOLOv5

# 이미지 처리
opencv-python>=4.6.0
Pillow>=9.0.0
rembg>=2.0.50  # 배경 제거
# mediapipe>=0.10.0  # 얼굴/체형 분석 (Python 3.13 미지원 - Python 3.11 또는 3.12에서만 설치 가능)
scikit-image>=0.21.0
# 얼굴 인식 및 성별 인식 라이브러리 (선택적)
# deepface>=0.0.89  # 얼굴 분석 및 성별 인식 (버전 충돌 주의)
# face-recognition>=1.3.0  # 얼굴 인식 (dlib 기반, 설치 복잡)

# 웹 프레임워크
streamlit>=1.25.0
flask>=2.2.0

# 데이터 처리
numpy>=1.21.0
pandas>=1.4.0

# CLIP 모델
transformers>=4.20.0
ftfy>=6.1.0
regex>=2022.0.0

# 유틸리티
python-dotenv>=0.19.0
tqdm>=4.64.0
psutil>=5.9.0

# 개발 도구
pytest>=7.0.0
black>=22.0.0

# 향상된 점수 계산 (색상 분석 등)
scikit-learn>=1.0.0  # K-means 클러스터링용

# AI 이미지 생성 (선택적)
# diffusers>=0.21.0  # Stable Diffusion 로컬 실행용 (M2 맥북 지원)
# accelerate>=0.20.0  # Stable Diffusion 가속화 (선택적이지만 권장)
# openai>=1.0.0  # DALL-E API 사용시
# huggingface_hub>=0.16.0  # Hugging Face Inference API 사용시 (일부 기능)
